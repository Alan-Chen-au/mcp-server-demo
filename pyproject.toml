[project]
name = "mcp-server-demo"
version = "0.1.0"
description = "MCP Server Demo with Streamable HTTP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.23.2",
    "fastmcp>=0.1.0",
    "uvicorn>=0.30.0",
    "starlette>=0.37.0",
]
